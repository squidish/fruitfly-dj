# npm 10.9.x's dependency resolver crashes on vitest 4's optional peer graph
# ("Cannot read properties of null (reading 'edgesOut')"). Legacy peer
# resolution sidesteps it so a plain `npm i` works out of the box.
legacy-peer-deps=true
